# tests

This directory holds validation assets for NOC.

## Intended contents
- `governance/` checks for policy and term consistency
- `runtime/` runtime and session validation
- `adapters/` adapter behavior validation
- `prompts/` prompt regression checks
- `integration/` end-to-end pilot validation

## Rule
Promotion should follow validation, not intuition.
